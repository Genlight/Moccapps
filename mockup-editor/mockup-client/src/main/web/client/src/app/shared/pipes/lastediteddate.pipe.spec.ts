import { LastediteddatePipe } from './lastediteddate.pipe';

describe('LastediteddatePipe', () => {
  it('create an instance', () => {
    const pipe = new LastediteddatePipe();
    expect(pipe).toBeTruthy();
  });
});
